import { Text, View } from 'react-native';

const Login = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-xl font-bold text-gray-900">로그인</Text>
    </View>
  );
};
export default Login;
